<!DOCTYPE html>
<html>
<head>
  <title>FirstAjaxProject</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>

<body>
  <div>
    <% if current_user.nil? %>
      <%= link_to "Log in!", new_session_url %>
    <% else %>
      <%= current_user.username %>
      <%= button_to "Log out", session_url, :method => :delete %>
    <% end %>
  </div>

  <%= yield %>
</body>
</html>
